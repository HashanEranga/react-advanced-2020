import React from 'react'

function Jsx() {
    return (
        <div>
            {/* return single element
                div/ section/ article/ Fragment
                Use camelCase instead of class 
                close every element 
                Formatting

            */}
            {/* creating an arrow finction also can be exported.
            but the default method is more prefarable */}
        </div>
    )
}

export default Jsx
